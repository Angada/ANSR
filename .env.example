# Copy to .env and fill. .env is gitignored.
PGHOST=localhost
PGPORT=5433
PGDATABASE=qansr
PGUSER=qansr
PGPASSWORD=qansr

PORT=4100

# Optional — provider keys (manage via secret manager in prod)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
